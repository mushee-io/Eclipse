# Eclipse frontend

This directory is deliberately not scaffolded with a fictional dashboard. Phase 8 will add the Next.js App Router client only after the vault, draw and user-decryption interfaces are compiled and integration-tested.
