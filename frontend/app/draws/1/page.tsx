"use client";

import { useState } from "react";
import { account, friendlyError, unlockDrawResult } from "../../../lib/eclipse";

const timeline=["Prize locked","Encrypted randomness generated","Confidential balances processed","Encrypted winner predicate evaluated","Shadow predicate evaluated","Draw finalized"];
export default function DrawOne(){const[result,setResult]=useState<{won:boolean;prize:string}>();const[status,setStatus]=useState("");const unlock=async()=>{try{setStatus("REQUESTING ACCESS");const u=await account();setStatus("SIGN PRIVACY REQUEST");const r=await unlockDrawResult(u);setStatus("RESULT READY");setResult(r);}catch(e){setStatus(friendlyError(e));}};return <section className="page"><p className="eyebrow">VERIFIED ON ETHEREUM SEPOLIA</p><h1 className="page-title">ECLIPSE<br/>DRAW #1</h1><div className="draw-grid"><article><p className="eyebrow">PUBLIC</p><dl><dt>DRAW DOMAIN</dt><dd>2³⁰</dd><dt>PARTICIPANTS</dt><dd>3</dd><dt>NETWORK</dt><dd>SEPOLIA</dd></dl></article><article><p className="eyebrow">PRIVATE</p><dl><dt>TOTAL SAVED</dt><dd>ENCRYPTED</dd><dt>RANDOM POSITION</dt><dd>ENCRYPTED</dd><dt>WINNER / PRIZE</dt><dd>ENCRYPTED</dd><dt>SHADOW OUTCOME</dt><dd>ENCRYPTED</dd></dl></article></div><div className="timeline">{timeline.map(x=><div key={x}><span>✓</span>{x}</div>)}</div><article className="result-card"><p className="eyebrow">YOUR RESULT</p>{result?<><h2>{result.won?"YOU WON.":"NOT THIS TIME."}</h2><p>{result.won?`Private prize: ${result.prize} cUSDT`:"Your principal remains yours."}</p></>:<><h2>████████</h2><p>Only your wallet can authorize decryption of its encrypted result.</p><button className="button primary" onClick={unlock}>UNLOCK RESULT</button></>} {status&&<small>{status}</small>}</article></section>}
