import { CONTRACTS, explorerAddress, explorerTx } from "../../lib/contracts";
import { LIVE_PROOF } from "../../lib/liveProof";
const cards=[["ECLIPSE VAULT",CONTRACTS.vault],["PRIZE RESERVE",CONTRACTS.prizeReserve],["ECLIPSE DRAW",CONTRACTS.draw],["YIELD SIMULATOR",CONTRACTS.yieldSimulator]] as const;
const checks=["Canonical confidential cUSDT deposits","Encrypted Zama randomness","TVL not used as random modulus","Aggregate TVL never publicly decrypted","Encrypted cumulative balance ranges","Encrypted weighted winner selection","Encrypted Shadow condition","User-specific result decryption","Uniform public claim path","Principal isolated from prize funds","Principal withdrawal after finalization"];
export default function Verify(){return <section className="page"><p className="eyebrow">LIVE PROOF · DRAW #1</p><h1 className="page-title">PRIVACY<br/>WITHOUT BLIND TRUST.</h1><p className="lede">Eclipse hides financial positions. It does not hide protocol execution.</p><div className="contract-grid">{cards.map(([name,address])=><a key={name} href={explorerAddress(address)} target="_blank" rel="noreferrer"><span>{name}</span><strong>{address.slice(0,8)}…{address.slice(-6)}</strong><small>VIEW ON ETHERSCAN ↗</small></a>)}</div><section className="verification"><div><p className="eyebrow">LIVE DARK CAPACITY DRAW</p><h2>REAL SEPOLIA.<br/>REAL FHE.</h2><p>3 participants · 2³⁰ fixed capacity</p></div><ul>{checks.map(x=><li key={x}><span>✓</span>{x}</li>)}</ul></section><div className="proof-table"><p className="eyebrow">TRANSACTION PROOF</p>{LIVE_PROOF.transactions.map(([label,hash,block])=><a href={explorerTx(hash)} target="_blank" rel="noreferrer" key={hash}><span>{label}</span><code>{hash.slice(0,12)}…{hash.slice(-8)}</code><small>BLOCK {block} ↗</small></a>)}</div><section className="split compact"><div><p className="eyebrow">WHAT THE CHAIN PROVES</p><p>Draw execution · contract transitions · fixed-capacity domain · encrypted computation · finalization · claims · principal withdrawal.</p></div><div><p className="eyebrow">WHAT IT DOESN'T REVEAL</p><p>Deposit amounts · aggregate TVL · winning position · winner identity · Shadow outcome · prize amount.</p></div></section><div className="manifesto single">VERIFY EVERYTHING WITHOUT SEEING EVERYTHING.</div></section>}
